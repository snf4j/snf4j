sudo: true

language: java

jdk:
  - oraclejdk8
  
branches:
  only:
  - master
  - travis_yml

after_failure:
  - pwd
  - cat snf4j-core/target/surefire-reports/*.txt
  - cat snf4j-core-slf4j/target/surefire-reports/*.txt